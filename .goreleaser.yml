version: 2

project_name: thrive

before:
  hooks:
    - go mod tidy

builds:
  - id: thrive-linux
    binary: thrive
    main: ./cmd/thrive
    goos: [linux]
    goarch: [amd64, arm64]
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.Version={{.Version}} -X main.Commit={{.ShortCommit}}

  - id: thrived-linux
    binary: thrived
    main: ./cmd/thrived
    goos: [linux]
    goarch: [amd64, arm64]
    env:
      - CGO_ENABLED=0

  - id: thrive-darwin
    binary: thrive
    main: ./cmd/thrive
    goos: [darwin]
    goarch: [arm64, amd64]
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.Version={{.Version}} -X main.Commit={{.ShortCommit}}

  - id: thrive-windows
    binary: thrive
    main: ./cmd/thrive
    goos: [windows]
    goarch: [amd64, arm64]
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.Version={{.Version}} -X main.Commit={{.ShortCommit}}

archives:
  - id: thrive
    ids: [thrive-linux, thrive-darwin, thrive-windows]
    name_template: "thrive_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - LICENSE
      - CHANGELOG.md

  - id: thrived
    ids: [thrived-linux]
    name_template: "thrived_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - README.md
      - LICENSE

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - Merge pull request
      - Merge branch

release:
  github:
    owner: thakurtpr
    name: thrive
  draft: false
  prerelease: auto
  name_template: "v{{.Version}}"
